[package]
name = "lumen-platform"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Platform capability types and data paths for LumenAsr"

[dependencies]
lumen_platform_suite = { workspace = true }
