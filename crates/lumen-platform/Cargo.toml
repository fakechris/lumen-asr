[package]
name = "lumen-platform"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Product facade over the shared lumen-suite platform types + LumenAsr data paths"

[dependencies]
lumen_platform_suite = { workspace = true }
